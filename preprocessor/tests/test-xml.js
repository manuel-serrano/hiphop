
const TOTO=function(args){}

const foo = <toto apply=~{function() {
   return MODULE() {
      PAUSE;
   }
}
}/>

const bar = <toto apply=${AWAIT(NOW(b))}>
  ~{
     if (i == 5 && a <b) {
	console.log(234)
     }
  }
  <toto/>
  <bubu>
   ~{ PAUSE; <tata/>}
  </bubu>

  <body>
     window.onload = function() {
       new hh.ReactiveMachine(MODULE() {
  	  PAUSE;
  	  ${TOTO()}
       })
     };
  </body>
</toto>

// erreur qui ne devrait pas avoir lieu.
  // <body>
  //    window.onload = function() {
  //    }
  // </body>
